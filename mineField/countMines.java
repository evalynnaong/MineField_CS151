package mineField;

/*public class countMines {
    public int countMines(Field field, int x, int y){
        int count = 0;
        int[] dx = {-1, -1, -1, 0, 0, 1, 1, 1};
        int[] dy = {-1, 0, 1, -1, 1, -1, 0, 1};

        for(int i = 0; i < 8; i++){
            int nx = x + dx[i];
            int ny = y + dy[i];

            if (nx >= 0 && y >= 0 && x < field.getSize() && y < field.getSize()){
                if (field.getTile(x, y).getMineStatus()) {
                    count++;
                }
            }
        }
        return count;
    }
}
*/
